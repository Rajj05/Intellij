import React from 'react';

// Simple smoke test - just verify React is working
test('React is defined', () => {
  expect(React).toBeDefined();
});

test('Basic math works', () => {
  expect(1 + 1).toBe(2);
});
