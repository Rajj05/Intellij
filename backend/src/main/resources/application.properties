# Server Configuration
server.port=8080

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/portfolio?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=n3u3da!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.portfolio=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# Stock API Configuration (Finnhub - get free key at https://finnhub.io)
# 1. Go to https://finnhub.io and sign up for free
# 2. Copy your API key and paste it below
# 3. Set stock.api.enabled=true to enable live prices
stock.api.key=d5vpplhr01qihi8nbea0d5vpplhr01qihi8nbeag
stock.api.enabled=true
stock.api.base-url=https://finnhub.io/api/v1

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173

# SpringDoc/Swagger Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha



